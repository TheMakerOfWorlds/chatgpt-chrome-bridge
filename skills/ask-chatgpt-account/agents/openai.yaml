interface:
  display_name: "ChatGPT Parallel Worker"
  short_description: "Parallel research with safe repository bundles"
  default_prompt: "Use $ask-chatgpt-account for standalone research, explicit files, or a reviewed sanitized repository bundle; preserve long-running Pro job IDs and retrieve generated files as local resources."
dependencies:
  tools:
    - type: "mcp"
      value: "chatgpt-chrome-bridge"
      description: "Context-isolated ChatGPT worker with local repository bundling, generated-file retrieval, durable Pro status, and browser inspection"
policy:
  allow_implicit_invocation: true
